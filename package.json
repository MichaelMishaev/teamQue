{
  "name": "queue-line-manager",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter shared build && pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter shared build && pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.33.2"
}
