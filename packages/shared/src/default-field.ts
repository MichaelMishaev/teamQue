/**
 * The permanent Independence Square court. It starts a fresh empty session
 * after the evening ends, while each closed session remains as its own history.
 */
export const DEFAULT_FIELD_NAME = 'כיכר העצמאות'
