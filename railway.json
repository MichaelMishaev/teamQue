{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["/apps/api/**", "/apps/web/**"]
  },
  "deploy": {
    "startCommand": "pnpm --filter api db:migrate && pnpm --filter api start"
  }
}
